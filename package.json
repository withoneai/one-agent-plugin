{
  "name": "@withone/one-agent-plugin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "generate": "node scripts/generate-platform-plugins.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "ajv": "^8.17.1"
  }
}
